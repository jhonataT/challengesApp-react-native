import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        height: 50,
        borderColor: '#fff',
        borderWidth: 1,

        marginVertical: 6,
        paddingHorizontal: 12,
        borderRadius: 4,

        color: '#fff',
    }
});